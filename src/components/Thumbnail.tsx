import styled from "styled-components";

const Thumbnail = styled.img`
  user-select: none;
  cursor: pointer;
  pointer-events: all;
  position: relative;
  max-width: 100%;
`;

export default Thumbnail;
